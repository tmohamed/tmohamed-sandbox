
mvn clean package -DskipTests

mvn clean package -DskipTests && docker compose build --no-cache && docker compose up

start docker deamon
minikube start --cpus=4 --memory=8192

eval $(minikube docker-env -u)
minikube docker-env -u --shell fish | source


# 1. Build Maven JARs
mvn clean package -DskipTests

# 2. Build Docker Images (Targeting Minikube Context)
docker build -t aggregator-service:1.0.0 ./aggregator-service
docker build -t service-a:1.0.0 ./service-a

# Step 3: Load Images into Minikube
minikube image load aggregator-service:1.0.0
minikube image load service-a:1.0.0


# Step 4: Verify Images in Minikube
minikube image ls | grep -E "aggregator|service"

kubectl apply -f k8s/
kubectl get pods -w


kubectl port-forward svc/aggregator-service 8080:8080

kubectl logs -l app=service-a --max-log-requests=10 -f --tail=1


for i in {1..9}; do curl -s http://localhost:8080/benchmark/grpc/1; echo ""; done

# Fish shell version of script:
for i in (seq 1 9); curl -s http://localhost:8080/benchmark/grpc/1; echo ""; end


# 2. Test gRPC
mvn test-compile gatling:test -pl load-tests -Dgatling.simulationClass=com.example.benchmark.GrpcSimulation


# Get the Direct Minikube Endpoint
minikube ip

# If using the Docker driver on macOS where minikube ip routes through Docker's internal bridge network, run:
minikube service aggregator-service --url


# Run In-Cluster Load Testing

# Test 150 RPS (6,000 calls over 30s)
kubectl run load-generator --image=fortio/fortio --rm -it -- load -c 10 -qps 150 -t 30s http://aggregator-service:8080/benchmark/grpc/1

# Test 200 RPS (6,000 calls over 30s)
kubectl run load-generator --image=fortio/fortio --rm -it -- load -c 20 -qps 200 -t 30s http://aggregator-service:8080/benchmark/grpc/1

# Test 300 RPS (6,000 calls over 60s)
kubectl run load-generator --image=fortio/fortio --rm -it -- load -c 30 -qps 300 -t 60s http://aggregator-service:8080/benchmark/grpc/1

# Test 500 RPS (6,000 calls over 120s)
kubectl run load-generator --image=fortio/fortio --rm -it -- load -c 50 -qps 500 -t 120s http://aggregator-service:8080/benchmark/grpc/1